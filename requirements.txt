fastapi>=0.111,<1.0
deepgram-sdk==7.5.0
twilio==9.10.9
uvicorn[standard]>=0.30,<1.0
openpyxl>=3.1,<4
openai>=1.93,<2
python-dotenv>=1.0,<2
# Exotel and Teler REST. Exotel has no officially maintained Python client;
# Teler has one, but it wraps only call creation -- no status fetch and no
# hangup -- and raises on any response field it does not declare. See
# app/telephony/providers/teler_provider.py.
httpx>=0.27,<1

python-multipart>=0.0.9,<1

